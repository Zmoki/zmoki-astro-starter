# Analytics/captcha default to on only for the deploy host's real production
# build of `main` (src/lib/deploy.ts). These are optional overrides of that
# default — set to "true" or "false"; leave blank to just use the default.
PUBLIC_ANALYTICS_ENABLED=
PUBLIC_CAPTCHA_ENABLED=
