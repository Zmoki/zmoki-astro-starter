# Cloudflare Pages redirects. Format: <from> <to> <status>
# Add legacy-slug or external redirects here as your site grows, e.g.:
# /feed/old-slug/ /feed/1-new-slug/ 301
# /resources/something/ https://external.example.com/ 302
