---
import { isoDate, formatLongDate } from "@/lib/dates";

export interface Props {
  datetime: Date;
}

const { datetime } = Astro.props;
---

<time datetime={isoDate(datetime)}>{formatLongDate(datetime)}</time>
