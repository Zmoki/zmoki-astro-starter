---
import BaseLayout from "@/layouts/BaseLayout.astro";
import Time from "@/components/Time.astro";

const { title, description, publishDate, contentModifiedDate } = Astro.props;
---

<BaseLayout
  title={title}
  description={description}
  publishDate={publishDate}
  contentModifiedDate={contentModifiedDate}
>
  <section class="space-y-4">
    <header
      class="border-zmoki-stone-200 flex flex-col gap-6 rounded-2xl border bg-white px-4 py-8 shadow-sm md:items-center md:p-16 md:text-center xl:p-20"
    >
      <h1 class="text-5xl font-semibold tracking-tight xl:text-6xl" set:html={title} />
      {
        description && (
          <p class="text-zmoki-stone-600 text-2xl xl:text-3xl" set:html={description} />
        )
      }
      <p class="text-zmoki-stone-500 order-first flex flex-col gap-2 text-sm md:flex-row lg:gap-4">
        <span>Updated on <Time datetime={contentModifiedDate} /></span>
      </p>
    </header>
    <section
      class="prose border-zmoki-stone-200 prose-headings:font-semibold max-w-none rounded-2xl border bg-white px-4 py-8 shadow-sm md:p-16 xl:px-28 xl:py-16"
    >
      <slot />
    </section>
  </section>
</BaseLayout>
